---
title: "Installation"
description: "Install langchain-adk with pip or uv. Add OpenAI, Anthropic, Google, MCP, or A2A provider extras. Python 3.10+ required."
keywords: ["install", "pip", "uv", "langchain-adk", "setup", "OpenAI", "Anthropic"]
---

## Basic install

<Tabs>
  <Tab title="pip">
    ```bash
    pip install langchain-adk
    ```
  </Tab>
  <Tab title="uv">
    ```bash
    uv add langchain-adk
    ```
  </Tab>
</Tabs>

## With an LLM provider

<Tabs>
  <Tab title="OpenAI">
    ```bash
    pip install langchain-adk[openai]
    ```
  </Tab>
  <Tab title="Anthropic">
    ```bash
    pip install langchain-adk[anthropic]
    ```
  </Tab>
  <Tab title="Google Gemini">
    ```bash
    pip install langchain-adk[google]
    ```
  </Tab>
  <Tab title="MCP">
    ```bash
    pip install langchain-adk[mcp]
    ```
  </Tab>
</Tabs>

<Info>
  **Python >= 3.10** is required.
</Info>
